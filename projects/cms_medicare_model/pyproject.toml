[project]
name = "cms_medicare_model"
version = "0.1.0"
description = "Dimensional model for CMS Medicare Claims (DE-SynPUF) using dbt + DuckDB"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "dbt-duckdb>=1.6.0",
    "pandas>=2.0.0",
    "requests>=2.31.0",
]

[dependency-groups]
dev = ["pytest>=7.0.0"]
